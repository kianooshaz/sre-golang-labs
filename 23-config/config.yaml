server:
  host: 0.0.0.0
  port: 8080
  tls: false
  read_timeout: 5s

log:
  level: info
  format: json

workers:
  - name: metrics
    queue_size: 1000
    enabled: true
  - name: alerts
    queue_size: 100
    enabled: false
